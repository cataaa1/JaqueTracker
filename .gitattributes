# Todo el texto se guarda con finales de línea LF en el repositorio, aunque
# Windows use CRLF en el disco. Sin esto los diffs muestran archivos enteros
# como modificados y el build de Cloudflare Pages (que corre en Linux) ve algo
# distinto de lo que ves vos.
* text=auto eol=lf

*.woff2 binary
